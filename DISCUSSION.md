1. I would have liked to make a shared types folder for the API and FE.
2. I would put the specialties in some kind of popup instead of listing them all out and making the column large.
3. Have a show number of rows per page option
4. Break out pagination, search, and table into it's own seperate component
5. Replace the title with a logo
6. Add better error handling for API calls
7. Move the API calls on the FE to a hook
8. I had set a limit for at least two characters for the debounced search, but realized we're searching single digit numbers as well.